.row
  .column
    p <%= @question[:label] %>
    .input-wrap
      <% for answer, index in @question[:answers] %>input#<%= @name %>_<%= index %> type="checkbox" name="<%= @name %>[]" value="<%= answer %>"
      label.inline for="<%= @name %>_<%= index %>" <%= answer %>
      <% end %>
